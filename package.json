{
  "name": "harmony",
  "private": true,
  "version": "0.1.0",
  "description": "Habits that help you return to yourself.",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @harmony/web dev",
    "build": "pnpm --filter @harmony/web build",
    "typecheck": "pnpm -r typecheck",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.2.7"
  }
}
